# journeyman

Python source of truth for Journeyman. See the [root README](../README.md) for the loop, API, eval, and how to run.

```bash
python -m pip install -e ".[dev]"
python -m pytest
python -m journeyman.demo.run --challenge frozen --mode offline
python -m journeyman.server
```
