/** Stage 6 stub. Red-team on LIVE is polish — do not run hold-out or write the journal. */
export function redTeamStatus(): { status: "stub"; live: boolean; note: string } {
  return {
    status: "stub",
    live: false,
    note: "RedTeam polish is stage 6. LIVE cadence is not armed.",
  };
}
